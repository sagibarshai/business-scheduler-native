import { StyledTitle, StyledWrapper } from "./styled";
import { Props } from "./types";

const Login = ({}: Props) => {
  return (
    <StyledWrapper>
      <StyledTitle>שלום שם !!!</StyledTitle>
    </StyledWrapper>
  );
};
export default Login;
